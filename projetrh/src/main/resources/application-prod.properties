# Profil production — secrets obligatoires via variables d'environnement.
# Le démarrage échoue si DB_PASSWORD ou JWT_SECRET sont absents.

spring.jpa.show-sql=false
logging.level.org.hibernate.SQL=WARN
